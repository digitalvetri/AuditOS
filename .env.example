## Copy to `.env` before running `npm run dev`.
## In mock mode, MSW intercepts every /api/* call and returns seeded data.
## Set to 'false' only when a real backend is wired at the same base URL.
VITE_MOCK_MODE=true
